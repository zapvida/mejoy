# MeJoy Web Wrapper

`apps/web` is the canonical workspace entry for the current Next.js product.

The application source still lives at the repository root during the transition
to a full monorepo layout. This wrapper lets the workspace expose a dedicated
web app package immediately, without forcing a risky file move before the mobile
foundation is stable.
